# Bastion reviewer policy — changes here require human review.
# Generated by `bastion github codeowners`; commit this into .github/CODEOWNERS (or your CODEOWNERS).
/bastion/ @jssblck
/.github/workflows/bastion.yml @jssblck
/CODEOWNERS @jssblck
/.github/CODEOWNERS @jssblck

# Hand-added beyond the generated block: the self-review workflow consumes the
# GitHub Action from the PR's own checkout (`uses: ./`), so the action
# definition is reviewer policy for this repository too.
/action.yml @jssblck
